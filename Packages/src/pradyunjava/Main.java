package pradyunjava;

public class Main {
    /** Classes and Interface name conflicts will happen
     *      mechanism is needed to fully specify class
     *  imports refereable like so
     *      if 2 imports have same name, 1 must be referred in variable itself
     *  make it easy to see if classes related
     *  easy to find in a certain package
     *  name conflicts avoided
     *  java.lang is already imported
     */

    public static void main(String[] args) {
	    javafx.scene.Node node = null;
    }
}
