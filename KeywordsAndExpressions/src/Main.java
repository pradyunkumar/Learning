public class Main {

    public static void main(String[] args) {
	// on IntelliJ, any orange word is reserved/keyword

    /* Expressions are building blocks of java programs
            built with values, variables, and operators, method calls  */

        // a mile is = to 1.609344 km
        double kilo = (100 * 1.609344);     //values, variables, operators
        //everything except data type is part of expression

        int highscore = 50;     //expression
        if(highscore == 50) {     //highscore == 50 is expression
            System.out.println("This is an expression.");   //expression
        }
    }
}
